# 老年用药安全指南（Docker smoke 语料）

## 用药核对

该文档仅服务于部署 smoke，不产生处方或个体化临床结论。运行中的索引器应能读取嵌套
目录、保留文件相对路径并将其作为证据来源。对老年患者的用药信息，实际产品应结合
完整病史、肾肝功能、并用药物、过敏史和临床医师判断进行审查；本 fixture 不包含可供
临床执行的剂量或换药指令。

## 证据可追溯性

每个可检索 chunk 需要关联文档标题、章节和来源类型。Docker 健康检查只在索引任务
成功结束后报告 ready，从而防止一个尚未具备本地检索能力的 API 被误认为可投入使用。
