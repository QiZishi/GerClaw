# 长期规划

> 项目长期记忆。完成度以生产证据为准；MVP 页面存在不等于生产后端能力完成。

## 一、项目长期目标

1. 交付患者端与医生端均可真实使用的 FastAPI + AgentScope + PostgreSQL/Redis/Qdrant 全栈。
2. 让 CGA、五大处方、用药审查、健康画像、语音、文档和 Skill 进入同一 Runtime Agent Harness。
3. 建立医疗安全、循证引用、权限/HITL、Trace、反馈/Bad Case/Eval 和数据加密闭环。
4. 以可重复 Development Harness、≤10 并发证据和 Docker 空卷运行完成生产验收，再进入临床试点。

## 二、产品原则

1. 医疗安全不可让步：禁止确定性诊断；红旗症状立即就医；所有医疗输出带免责声明。
2. 无证据不输出建议：本地 RAG 优先，必要时联网，引用必须可追踪，失败必须 fail closed。
3. 适老化不是简单放大：患者端正文≥18px、控件≥48px、高对比、语音优先、步骤清晰。
4. 访客可用但不越权：访客无需登录体验核心功能；账号、角色、患者授权和资源归属全链路隔离。
5. 模型和工具不可信：输入、输出、上传、搜索、MinerU、缓存和向量 payload 都要 schema 与策略校验。
6. 真实执行优先：mock、静态示例、历史截图或“应该可用”都不是完成证据。
7. 模块化但不做 Demo：每个模块必须有 Protocol、生产实现、错误语义、测试、README 和真实集成证据。
8. 只记录必要审计信息：不存 Chain-of-Thought，不在日志/Trace/向量库泄露 PHI 或密钥。

## 三、模块交付追踪日志

### [0014] 生产基础设施与可观测后端骨架

FastAPI、PostgreSQL、Redis、Qdrant、JWT scope/tenant、限流、加密、Trace/feedback 基础与 Docker 编排完成；独立审阅复现单元、真实依赖、迁移、认证隔离和外部 Provider 后 PASS。

### [0015] 本地医学知识库 Agentic RAG

完成安全 Markdown 解析、分块、embedding/lexical、Qdrant RRF、rerank、generation fencing、增量/撤回/中断恢复和 AgentScope tool；真实全量语料与对抗并发由独立审阅 PASS。

### [0016] AgentScope Agent Harness 与 SSE

完成 AgentScope ReAct、上下文、三模型主备、本地证据、SSE、session lease、Trace 重放和原子成功/失败终态；独立审阅对诊断、引用、事务和断流边界 PASS。

### [0017] Memory 与健康画像引擎

完成加密健康事实、跨会话画像、用户 evidence 约束、否定/冲突/重大事件、无 PHI Qdrant revision vector、AgentScope Mem0Middleware 和精确补偿；2026-07-15 06:55 独立 PASS。

### [0018] Search 联网医疗证据

完成 AnySearch→Tavily、来源分级、query 脱敏、SSRF/DNS/redirect 防护、不可信网页隔离、引用和 PHI-free Trace；真实 provider 与 AgentScope `web_search` 由独立审阅 PASS。

### [0019] Skill 注册执行与前端集成

完成四个内置 Skill、自定义 Markdown/ZIP、自然语言草稿、加密版本、会话选择、安全策略和 AgentScope viewer。最终独立审阅复现真实模型生成→注册→Skill viewer→RAG→引用→免责声明→Trace，2026-07-15 21:50 PASS。默认 `349 passed, 31 skipped`、coverage 80.02%；真实依赖 `370 passed, 10 deselected`、coverage 87.05%；知识库 436 文档、39,837 chunks、0 失败。

### [0020] 生产交付基线与 Development Harness

完成生产 PRD、59 项需求矩阵、事实 README/路线、统一 quality modes、动态文档验证、负向门禁和 CI workflow。quick 为 `349 passed, 31 skipped`、coverage 80.02%，真实依赖为 `370 passed, 10 deselected`、coverage 87.06%；Alembic upgrade/check、安全扫描、前端 build、Docker image 和本地浏览器 smoke 均有真实证据。独立审阅于 2026-07-15 22:41 CST 给出无 P0/P1 阻断的 PASS。

### [0021] Runtime Permission/HITL/Tool Registry 基础

完成 Runtime 基础合同：服务端 PermissionEngine、受治理 Tool Registry 与一次性 permit、持久化审批状态机和加密 version-bound checkpoint、执行预算/全周期 watchdog、审批 SSE/PHI-free Trace，以及审批读取/决定/取消 API。真实 PostgreSQL/Redis/Qdrant 集成门禁为 `402 passed, 10 deselected`、coverage `87.19%`；backend 门禁为 `377 passed, 35 skipped`、coverage `80.04%`。独立审阅确认无 P0/P1。临床副作用恢复 executor、多智能体临床复核与审批前端 UI 仍分别由后续业务 workflow 和 0022 前端计划完成。

## 四、进度总览表

| 生产模块 | 状态 | 里程碑/证据 | 下一步 |
|---|---|---|---|
| Development Harness | ✅已完成 | 0020 quick/integration/security/migration/docker/e2e，独立 PASS | CI 平台结果仍以 push 后为准 |
| FastAPI/数据基础设施/Trace | ✅已完成 | 0014 独立 PASS | 在新临床表继续复用 |
| Agentic RAG | ✅已完成 | 0015；436 文档、39,837 chunks | 维持增量索引和引用评测 |
| Runtime Agent Harness 核心 | 🚧部分完成 | 0016 ReAct/SSE/取消/原子终态；0021 权限、持久化 HITL、工具 permit、预算/checkpoint 与 Trace，独立 PASS | 0022 审批前端；后续临床 workflow 接入恢复 executor/复核 |
| Memory/健康画像引擎 | 🚧部分完成 | 0017 后端独立 PASS；`apps/mvp` 已通过受限 BFF 真实读取当前访客自身健康记录，并可确认/忽略其待确认事实 | 0025 真实账号/RBAC、患者授权与医生跨患者查看；0027 全前端验收 |
| Search 联网证据 | ✅已完成 | 0018 独立 PASS | 持续来源质量评测 |
| Skill | ✅已完成 | 0019 独立 PASS | 后续临床 Skill 复用 |
| Voice 后端 | 🚧部分完成 | `apps/mvp` 的服务端 BFF 已用非公开环境变量调用真实 MiMo ASR/TTS；请求采用严格 Zod 合同、WAV/MP3 与音色 allowlist、流式体积上限、ASR 30 秒/TTS 10 秒取消超时、安全阀和脱敏错误。FastAPI 已新增独立 `modules/voice`：`voice:use` scope、共享限流、受限 WAV/MP3 ASR、24kHz 单声道 PCM16 SSE TTS、稳定错误和无持久化音频/文本；离线 provider mock、路由和权限测试已通过。MVP BFF 尚未迁移到 PCM16 流，避免影响现有播放控制。 | 完成 BFF→FastAPI PCM16 迁移、provider 审计指标与真实端到端 ASR/TTS 评测 |
| Privacy 独立模块 | 🚧部分完成 | 核心 Chat 安全规则已有 | 0022 统一输入/输出/PHI 策略 |
| MinerU Document | 🚧部分完成 | Next.js BFF 已真实完成签名上传、轮询和 Markdown 下载；FastAPI 已实现加密登记、tenant/actor/session 绑定、撤销擦除和 JSON 不可信数据上下文，浏览器仅传 UUID。23 项相关单测、生产浏览器和独立复审均通过 | 私有向量 RAG、长文档检索、跨会话保留、医生授权与独立病毒扫描 |
| CGA 后端闭环 | 🚧部分完成 | PHQ-9、SAS 与 PSQI 数值自评均有版本化服务端状态机、真实 API 和患者端链路；PSQI 5J 可选说明单独加密保存，不参与分数、历史摘要或导出。48 个题干和 28 个去重选项的版本绑定预录制 WAV 已随 manifest 接入，缺失时才实时 TTS 兜底；老年模式在新题出现时自动尝试一次朗读，且可暂停、继续、停止。完成后的服务端报告可导出 Markdown，患者可查看本人最近 10 份完成报告。认知量表与医生授权未实现 | 0023 补齐 Mini-Cog/MMSE 与授权 |
| 风险预警/慢病管理/情感陪伴 | 🚧部分完成 | Chat Runtime Harness 已对红旗症状安全短路；患者端消费受验证的 SSE `safety_notice`，以单张适老红色紧急卡展示并隐藏干扰操作。Chat 红旗与 CGA 即时安全/高风险随访现在可原子生成加密、本人范围的告警并由本人幂等确认；`workflow=companion` 已作为真实受控后端 workflow 接入，禁用长期健康 Memory、RAG、联网、Skill 与上传资料，仍保留同会话加密聊天上下文和红旗短路。尚未接入慢病指标或用药事件，且无医生通知/跨患者队列；慢病管理、情感陪伴前端入口、用户可配置记忆偏好和人工升级仍未实现 | 0023、0030 继续扩展真实 workflow、安全边界与双端 UI |
| 五大处方后端闭环 | 🚧阶段 A | 已删除本地 mock 报告；患者端可通过真实 BFF/API 保存并恢复加密的最小信息收集，但不产生处方或生活方式建议 | 0029 补 Runtime Trace/PHI 策略、审核规则、医生批准与授权 |
| 用药审查规则 | ❌未实现 | 已有真实、无临床结论的信息收集入口；尚无 DDI/Beers/剂量或重复用药规则 | 0029 待医学团队提供可追溯规则、许可、版本与审核 |
| 账号/RBAC/患者授权 | ❌未实现 | 仅访客短期 JWT | 0025 |
| Feedback/Bad Case/Eval | 🚧部分完成 | Trace/基础反馈与加密 Bad Case 已有；已加入 6 个合成、人工审阅的确定性高风险安全 golden case，评测不回放真实用户输入且不调用模型/RAG | 0026 补授权脱敏晋升、模型/RAG 评测、趋势与指标闭环 |
| 前端全功能真实接入 | 🚧部分完成 | Chat/Skill 已接后端 | 0027 清除 mock、四断点 E2E |
| ≤10 并发/性能报告 | 🚧部分完成 | Docker Compose API 的确定性高风险安全短路 SSE 已通过 10 并发：10/10 done、失败率 0、p50 153ms、p95 154ms，且 Trace/消息/跨访客隔离均验证；不代表模型/RAG/临床 workflow 或千级容量 | 0026 补完整 workflow、取消/限流/幂等的统一报告 |
| Docker 最终运行交付 | 🚧部分完成 | 基础 compose/API image 可构建 | 0028 空卷/health/重启/E2E |

状态图例：❌未实现、🚧部分实现/进行中、✅有生产证据并独立通过。
