# 0034 — 前后端模块完备性审计

> 创建：2026-07-17 | 优先级：P0 | 状态：审计完成，按既有计划实施 | 依据：`goal-objective.md`、`gerclaw设计要求.md`

## 范围与结论

本次审计覆盖 `apps/api/src/gerclaw_api/modules` 的 19 个模块、`apps/mvp`、活跃执行计划、需求矩阵、真实 Compose 回归及已有 MinerU 病例 PDF 验证。所有模块目录均已有 `AGENTS.md` 与 `README.md`；不以目录、Mock、静态页面或本地示例作为完成证据。

前端第一阶段可按既有浏览器验收结论继续进入后端阶段：布局、加载与语音控制不应再作为阻断后端开发的理由。它仍不能被表述为完整产品交付，因为临床 workflow、账户入口及授权工作流尚未有真实后端终态可呈现。

## 后端模块状态

| 模块 | 审计结论 | 必须继续的工作 |
|---|---|---|
| agent_harness / runtime / tools | 核心 ReAct、SSE、取消、预算、审批和 Trace 已实现 | 临床 workflow 的恢复执行、复核与审批接入 |
| rag / search / skill | 本地知识库 RAG、联网证据与 Skill 核心已实现 | 私有文档 RAG、更多来源安全和持续评测 |
| cga | PHQ-9、SAS、PSQI 有确定性持久化闭环 | Mini-Cog/MMSE 的人工复核、医生授权和历史比较 |
| document / input_output / voice | MinerU 已使用既有病例 PDF 完成真实解析；加密登记与语音后端已存在 | 私有长文档检索、病毒扫描、BFF 到 FastAPI PCM16 迁移和真实 ASR/TTS 评测 |
| privacy_redaction | 搜索/TTS 脱敏、ASR/MinerU outcome audit、模型 prompt 内存投影与持久化 audit 已实现 | 模型/MinerU 同意、字段级分类、用户透明度与生命周期 |
| identity | 本地账号、会话、密码、刷新轮换、Cookie/CSRF 已实现 | 登录/注册 UI、医生资质、患者授权及撤回事实源 |
| memory | 本人范围健康画像和受限前端读取已实现 | 账号迁移、医生授权读取及生命周期控制 |
| risk_alert | Chat/CGA 风险可原子落库并由本人确认 | 慢病/用药来源、通知升级、医生队列和前端入口 |
| chronic_care | 加密自述病情与测量账本、无临床含义趋势已实现 | 经审核阈值/目标/提醒、授权医生与真实前端 workflow |
| companion | 后端安全陪伴 workflow 已实现 | 前端入口、用户偏好、人工升级与授权 |
| prescription / medication_review | 仅有真实、加密、版本化的最小信息收集；不产生建议 | 经审核模板/规则、RAG 证据、四重校验、医生批准、报告与导出 |
| evals | 合成安全/隐私/RAG 回归与≤10并发安全短路证据已存在 | 临床规则、模型、OCR/ASR 与 Bad Case 晋升的授权评测 |

## 不可绕过的产品/合规输入

以下不是工程人员可安全猜测的空白，确认前保持 fail closed：

1. 五大处方通用、去标识 JSON schema；DDI/Beers/剂量/重复用药规则的来源、许可、版本、审核人与复审日期。
2. 医生资质激活、患者授权资源粒度、有效期/撤回、紧急访问和审计保留策略。
3. 账号验证方式、找回/MFA/风控，以及访客数据迁移是否允许和如何确认。
4. Mini-Cog/MMSE 绘图、动作、观察条目的授权人工审核人、证据记录与患者/医生可见性。

## 开发顺序

1. 继续不依赖上述医学决定的安全治理：数据分类与 OCR/ASR/结构化字段评测扩展。
2. 完成身份入口和已确认的账号基础能力；未获资质和患者授权前，医生临床权限保持关闭。
3. 在收到第 1 项医学资料后，按 0029 实现确定性规则、证据、HITL 和报告闭环；病例 PDF 仅作为受控输入/关键证据，不作为公共知识库。
4. 随每个后端真实终态接入患者/医生前端，最后执行跨端 E2E、空卷 Docker 与≤10并发验收。

## 验收纪律

- 每项“已实现”必须有对应代码、契约测试与实际运行证据。
- 临床规则和患者数据不写入日志、Trace、评测集或回复；病例资料不得被复制为默认模板或 few-shot 示例。
- 模块需求必须同时更新 `docs/REQUIREMENTS_MATRIX.md`、长期规划和对应模块文档。
