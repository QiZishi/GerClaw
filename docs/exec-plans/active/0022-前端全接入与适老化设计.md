# 0022 — 前端全接入与适老化设计

> 创建：2026-07-15 | 优先级：P0 | 状态：进行中 | 唯一客户端：`apps/mvp`

## 1. 目标

把已有 FastAPI/AgentScope 能力做成可真实操作、可理解、适合老年人的 Web 界面。`apps/web` 为空，不另建第二套客户端；`apps/mvp` 继续作为 Next.js BFF 前端，并以服务层连接后端。

## 2. 设计决定

- **对象与单一任务**：老年患者在 30 秒内发现语音或文字入口并开始一次安全健康咨询；医生在同一工作台迅速辨认会话、证据和待处理事项。
- **视觉方向**：专业冷蓝（医生）/亲和暖蓝（患者）的“诊间台 + 家庭健康状态带”。状态带以文字、图标和颜色同时表达联网、语音、审批和安全状态，是唯一强调性的视觉签名；其余区域克制、可读、避免模板化装饰。
- **排版与响应式**：继续使用系统中文字体；桌面三栏、1024px 收起右栏、768px 抽屉侧栏、手机单列；患者老年模式正文不小于 18px、操作目标不小于 48px。

## 3. 范围

1. 审计并修复 `apps/mvp` 的布局、按钮响应、加载/空/错误/离线状态与键盘可达性。
2. 打通现有 FastAPI 聊天/SSE、会话、Skill 与 0021 请求方审批 API；审批请求必须有可见状态、刷新、取消与正确的无权限提示，不能假装已执行。授权人查看详情、批准/拒绝必须建立在真实账号/RBAC 上，随 0026 账号计划交付，禁止把本地“医生模式”当成授权身份。
3. 整合患者/医生入口，统一三栏和移动端行为；保留所有外部访问于 `services/` + BFF allowlist，边界用 Zod 校验。
4. 清除 UI 中把尚未实现的 CGA、处方、用药审查、文档/语音能力伪装成成功的路径；无后端实现时给出明确“暂未接入/可继续对话”的安全降级。
5. 使用真实 headless browser 覆盖访客→患者、核心按钮、错误/离线和不同断点；每轮前端改动跑 lint/build。

## 4. 非目标

- 不在本计划虚构 CGA、五大处方、用药审查、Voice 或 MinerU 的后端业务能力；它们留给 0023+ 对应后端计划。
- 不迁移到空的 `apps/web`，也不复制一套前端。
- 不绕过 BFF、访客身份或后端的 HITL 权限判断。

## 5. 验收标准

- [ ] 首页、侧栏、聊天输入和右侧面板在四个断点均没有遮挡，键盘焦点和文字标签可用。
- [ ] 患者模式满足 ≥18px 正文、≥48px 可操作目标、高对比度及语音优先入口；医生模式保持高信息密度但不牺牲可读性。
- [ ] 每个可见按钮有真实效果或明确禁用/说明；无后端、网络失败、空会话、流式中断和高风险提示都有可恢复状态。
- [ ] 已接入功能（聊天、Skill、审批）只经 `services/` 与 BFF allowlist；响应按 Zod schema 校验，不展示技术错误或伪成功。
- [ ] 0021 审批请求可在聊天流中呈现，请求人能够查看状态并取消；普通访客不能决定审批或读取敏感参数/执行 token。授权人工作台在 0026 真实账号/RBAC 建立后验收。
- [ ] `npm run lint`、`npm run build` 与浏览器审阅通过；独立审阅无 P0/P1 后再归档。

## 6. 预定证据

- eslint、Next production build 与现有前端必要测试。
- Playwright snapshot/screenshot：桌面、平板、手机，及访客/患者状态。
- BFF/API 负向演练：未配置后端、网络失败、审批无权限和可恢复提示。

## 7. 实施记录

- 2026-07-15：`apps/mvp` 确认为唯一前端；完成聊天/会话/Skill/审批请求方的服务接入第一步，并将未接入的 CGA、处方、用药审查与健康画像入口改为明确安全降级。
- 2026-07-15：补齐真实设置面板，患者适老模式固定浅色高对比外观；移除右栏与引用列表中的示例报告、示例来源，空状态不再伪装为业务结果。
- 2026-07-15：移除 MinerU 异常后返回 mock Markdown 的伪成功路径；按签名上传协议接入提交、PUT、轮询、Markdown 下载，增加服务端配置、Zod 响应校验、URL host allowlist、请求/响应流式字节上限、失败重试状态与契约测试。官方轻量 API 不支持 MD/TXT，因此二者改为受限本地读取并明确标注；在后端文件 ID/会话绑定落地前，解析结果仅供预览，不再伪称已加入智能体上下文。
- 2026-07-16：去除流式光标、思考点和 `animate-pulse` 的持续明暗闪烁，适老模式旋转载入降速；语音朗读统一为真实 TTS 状态，支持取消准备、暂停、继续、停止，并保证同一时间只播放一段。CGA 不再默认自动朗读，题目朗读同样提供独立停止控制。
- 2026-07-16：使用无敏感测试 PDF 完成 MinerU 官方接口与应用内 production 双重实测：签名提交成功、原始二进制 PUT 200、轮询完成、CDN Markdown 命中预期标记；实测补充签名 OSS host allowlist，并移除会破坏签名的自动 `Content-Type`。
- 2026-07-16：患者适老消息工具栏改为带文字的 ≥48px 操作，侧栏会话选择与置顶/删除拆分为键盘可达按钮；全局医疗免责声明提升至 ≥18px。toast 改为可关闭、默认按文字长度保证阅读时间且尊重调用方显式时长；反馈弹窗严格区分取消与提交并保存评价文字。
- 2026-07-15：移动端患者菜单与关闭、桌面折叠后的展开/新建以及右侧面板复制/关闭均补齐可见文字和 ≥48px 点击目标；修复移动抽屉“收起”无效、抽屉固定窄宽导致标题重叠、从抽屉打开设置形成双层遮罩的问题。右侧面板调宽支持键盘方向键/Home/End，空 CGA 面板改为真实未生成说明。
- 2026-07-15：右侧报告内容改为随会话持久化并与 `panelType` 配对；打开任意新面板先清空瞬时全局内容，再从当前会话恢复匹配报告。历史会话没有已保存报告时只显示空态，禁止把上一会话或另一类报告串线为 CGA/处方结果。
- 2026-07-15：文档上传移除人为 300ms“上传中→解析中”阶段和重复的开始 toast，统一为稳定的提交解析状态；文件类型/大小错误改为保留到用户主动关闭。文档预览在患者适老模式下为缩小、放大、复制提供文字标签，缩放会真实覆盖 Markdown 正文/标题/表格字号且最小保持 18px，剪贴板失败给出可操作提示；解析完成时间随真实解析结果记录，旧数据不再用组件打开时间冒充。
- 2026-07-15：删除所有无生产入口的旧 mock 前端（本地处方生成、假审批/历史、假用药审查、延迟角色切换等），保留仍在真实页面使用的安全/导出基础组件；导出临时渲染由固定等待 800ms 改为同步提交并等待真实字体/帧就绪。
- 2026-07-15：处方/用药报告生成失败时不再拼接并保存通用医疗建议或发送“已生成”操作卡，而是保留对话信息并明确提示如何重试；健康画像内部兜底不再声称已有档案。CGA 解读失败时仅展示真实本地量表计分并标注“解读未生成/不是诊断”，右栏统一称为“评估结果”。
- 2026-07-16：聊天流中的请求方审批卡在首次打开和手动刷新时均以服务端状态为准；未确认前明确说明不会执行且不提供取消，读取/取消失败保留可恢复的可见提示，并以请求版本防止慢响应覆盖最新状态。患者适老模式下补齐 ≥18px 说明和 ≥48px 文字按钮；前端不提供批准/拒绝入口，不把本地角色当作授权身份。独立复审与 lint、production build、diff 检查均通过。
- 2026-07-16：工具调用卡片补齐稳定的秒级真实耗时（运行中随 `startedAt` 更新、结束后固定），并在“减少动态效果”下停止 spinner 与展开动画；通用失败卡在折叠摘要和详情中均显示错误，无参数/结果/错误详情的卡片不再伪装为可展开。浏览器 fixture 实测运行态耗时递增；未配置后端时仍明确提示服务不可用，不伪装连接成功。
- 2026-07-16：production headless browser 已覆盖患者首页 1440px、1024px、767px 与 390px 宽度：768px 以下转为带文字的“菜单”抽屉入口，390px 单列下输入区、图片/文件/技能/处方/评估/语音入口与免责声明均未遮挡；浏览器控制台在未选会话的核心路径无错误。截图保留在本地 `output/playwright/` 作为回归证据。
- 2026-07-16：独立 production 复审复测四个断点通过：移动抽屉支持 Escape 关闭，语音拒绝权限与处方/CGA 未接入工作流均有明确可关闭/可恢复说明。技能后端未配置时 BFF 会返回 503，前端如实降级为“没有匹配的可用技能”空态；该外部服务不可用已记录，不是未捕获的前端异常。
- 2026-07-16：流式连接异常不再覆盖已接收的回复：保留片段并追加“未完成、未通过最终安全校验、请重新生成”的医疗安全说明；无片段时只显示固定非技术恢复文案，不向患者暴露 SSE 原始错误或 trace。错误态只保留可见的“重新生成”，不提供朗读、反馈、导出等完成态操作。production browser fixture 已复验部分回复与未配置后端 503 的安全降级，独立审查通过。
- 2026-07-16：聊天输入与 CGA 录音波形移除静音时按时间切换的伪活跃条，仅反映真实音量；录音时长从 100ms 高频重绘改为整秒更新。独立 mock-media browser fixture 复验 28 根静音波形连续稳定，真实音量 analyser 与停止/取消控制不受影响。
- 2026-07-16：复核 MinerU 前端链路为真实的签名上传、轮询与 Markdown 下载，`npm run test:document` 通过 5/5。Runtime Harness 当前明确拒绝非空 `uploaded_files`，故前端继续如实标注“已解析、未加入上下文”，不把预览文本绕过文档登记/所有权/会话绑定而注入模型；长期规划与产品规格已同步为该实际边界。
- 2026-07-16：文档解析的固定 50→70 伪进度已移除。因为浏览器仅等待一次 BFF 请求，无法可靠获知签名上传和远端解析百分比，文件标签改为稳定的“正在解析文档”文字与 `aria-live` 状态播报；不再用持续动画或数字进度误导用户。production browser 以本地 MD 文件复验完成态无进度条且无控制台错误。
- 2026-07-16：语音播放的退出控制改为调用全局活跃播放器，而非 `ChatArea` 自己未播放过音频的 hook 实例；消息朗读、CGA 朗读与工作流退出现在共享同一个实际停止入口，避免界面退出后语音继续播放。新增 `npm run test:audio` 覆盖跨实例替换、工作流停止与幂等停止，独立 production smoke、lint、build、diff 检查均通过。
- 2026-07-16：移除 `chatAction` 的历史本地开场与信息收集模拟路径；即使持久化 UI 状态或没有 `onStartAction` 的嵌入式输入组件直接触发临床入口，前端也只会明确说明工作流尚未接入，不会创建会话、伪造 AI 回复或显示本地报告。文件上传不再因旧临床状态被错误改为打开空预览面板。
- 2026-07-16：toast 对同一可见文案在停留期内复用同一张卡片并重置停留时间，不再因连续点击堆叠相同提示和重复进场动画；所有 toast 继续保留可见“关闭”操作与按文案长度计算的阅读时长。
