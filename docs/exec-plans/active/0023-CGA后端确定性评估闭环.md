# 0023 — CGA 后端确定性评估闭环

> 创建：2026-07-16 | 优先级：P0 | 状态：进行中 | 前置：0022 前端安全降级

## 1. 目标

把 CGA 从 `apps/mvp` 中的本地演示状态迁移为 FastAPI 可持久化、可恢复、可审计的临床筛查 workflow。计分、分级和风险标记必须由确定性规则完成；LLM 仅可在后续阶段承担对话引导，不能改变题号、答案或分数。

## 2. 分阶段范围

### 阶段 A（本变更集）

PHQ-9 最小真实纵切面已交付：版本化量表定义、严格答案校验、幂等状态机、确定性计分/报告、PHQ-9 第 9 题即时高风险提示、租户/本人隔离和加密持久化 API。前端仍待在 API 契约上由 `apps/mvp/services/` 接入。

### 阶段 B（后续同一计划）

在同一引擎契约上补齐 PSQI、Mini-Cog、MMSE；接入安全的对话引导、疲劳暂停、医生授权查看、报告导出和历史对比。未实现的量表不得出现在 API 的可用列表或前端“已可评估”入口。

## 3. 设计与安全约束

- 所有量表定义由后端版本化、只读地提供；客户端不得提交题目文本、分值或总分。
- 所有读写按 JWT `tenant_id + actor_id` 绑定。评估答案、报告和风险原因使用 `EncryptedJSON`，日志/Trace 不写入题干或答案。
- API 只输出“筛查结果”，永不诊断。报告始终附带“不能替代临床诊断”的免责声明。
- PHQ-9 第 9 题非零须在保存答案的响应中立即返回“需要立即安全评估”提醒；总分 ≥20 是独立的“尽快临床随访”状态，二者不得混淆。前者明确建议立即联系家人、医生或当地紧急医疗服务。
- 从服务端当前题目提交答案；拒绝跳题、未知题目、越界分数、已完成后继续答题和陈旧版本写入。重复提交同一答案为幂等，修改答案则安全重算。
- 量表与 API 输入输出均使用 Pydantic `extra="forbid"`；数据库迁移可回滚，所有 PHI 列加密。

## 4. 验收标准

- [ ] `GET /api/v1/cga/scales` 仅返回已有完整服务端 workflow 的公开定义与版本；患者端只展示已通过前端验收的量表。
- [ ] 可开始、读取、依序回答、断点恢复并完成一次 PHQ-9；所有状态/得分由服务端产生。
- [ ] 计分覆盖 0/4/5/10/15/20/27 边界；第 9 题风险和总分高风险均即时且可测试。
- [ ] 答案/报告数据库原始值为加密密文；另一主体和另一租户无法读取或写入评估。
- [ ] API、模块、repository、迁移和文档都有必要测试；执行对应 pytest、ruff、mypy、Alembic upgrade/check，并由独立审阅复测。
- [ ] 前端接入前保持 0022 的明确降级，不能把阶段 A 误称为已完成全部五项 CGA。

## 5. 非目标

- 阶段 A 不生成 LLM 医学解读、不调用搜索、RAG 或 TTS，不做诊断或处方建议。
- 阶段 A 不开放医生跨患者访问；该能力依赖 0025 的真实账号/RBAC/患者授权。
- 不把本地 `localStorage` 或前端静态分数作为服务端事实来源。

## 6. 实施记录

- 2026-07-16：审计确认现有 `workflow="cga"` 仅禁用聊天搜索，尚不存在量表、评估持久化、评分或报告 API；0022 因而维持 CGA 安全降级。决定先实现 PHQ-9 的安全纵切面，并把另外四项量表列为明确后续阶段。
- 2026-07-16：PHQ-9 已具备版本化后端定义、服务端题目顺序、`revision` 并发围栏、加密 `cga_assessments` 持久化及 `cga:read/write` JWT scope。已提供 `GET /cga/scales`、开始、读取/恢复、顺序答题、完成和报告 API；第 9 题自伤信号与总分 ≥20 的临床随访状态明确分离。真实本地 PostgreSQL 已迁移至 `a72c814f2021`，ASGI 演练完成 9 题、即时风险、报告读取与密文列验证；未完成即完成返回可恢复 409，状态不变。
- 2026-07-16：阶段 A 尚未完成前端接入、医生授权查看、导出及 SAS/PSQI/Mini-Cog/MMSE。这些能力继续保持未接入状态，不得由页面或 API 暗示为已实现。
- 2026-07-16：`apps/mvp` 已经由 Zod 验证的 `services/gerclaw/cga.ts` 与 BFF allowlist 接入真实 PHQ-9。患者端仅展示服务端当前题、四个服务器选项、保存状态、即时安全提示和筛查免责声明；刷新按 assessment ID 恢复，其他本地静态量表仍不可启动。production browser 实测第 1 题答题后服务端推进至第 2 题，控制台无错误。
- 2026-07-16：发现产品规格称“ SAS 焦虑量表”，而历史静态前端数据以 `scale_sas` 标识实现 GAD-7，二者不是同一标准化量表。该医学内容冲突在确认权威问卷/版权与计分规则前不得接入生产 API 或重新开放前端入口。
- 2026-07-16：PHQ-9 前端恢复边界已复审：失效/越权服务端 ID 与损坏 localStorage ID 均清除后新建并如实提示；网络失败保留断点重试。两个退出入口均说明进度已保存且不清理后端或 localStorage。隔离 production browser 会话实测退出确认、重新进入和第 1 题恢复，控制台 0 errors/0 warnings；独立审查 PASS。
- 2026-07-16：复核本地权威问卷后确认焦虑量表为 SAS，而非历史前端错误标识的 GAD-7。已新增独立、无模型依赖的 SAS 20 题确定性评分核心（5 项反向计分、粗分×1.25 half-up 标准分及四档分级）与边界/恶意输入测试。
- 2026-07-16：CGA 状态机已泛化为 PHQ-9/SAS，题目与选项始终由服务端定义，数据库 `current_position` 上限经 `a92c814f2023` 迁移扩至 30；`GET /cga/scales` 和开始/答题/完成/报告 API 已真实支持 SAS。保留旧 PHQ-9 加密报告的 `score_max=27` 读取兼容。真实 API 完成 20 题 SAS 演练（raw 80、standard 100、无错误即时自伤信号）；`30 passed`、ruff、mypy 与 Alembic check 通过。
- 2026-07-16：患者端 CGA 已改为读取真实服务端量表目录，不再固定为 PHQ-9：患者可分别开始/恢复 PHQ-9 或 SAS，题数、选项、计分上限和 SAS 粗分/标准分展示均来自受 Zod 验证的后端响应。生产浏览器实测 SAS 量表选择、20 题布局与第 1 题保存到第 2 题推进，控制台 0 errors/0 warnings。PSQI、Mini-Cog、MMSE、报告导出、医生授权查看与历史比较仍未实现。
