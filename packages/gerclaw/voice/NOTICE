This package follows the provider/lifecycle separation of dsh-talk 0.1.2
(Apache-2.0) and ports GerClaw's MiMo streaming contract. It adds the
SiliconFlow OpenAI-compatible PCM provider and never persists source audio.
