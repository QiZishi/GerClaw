# GerClaw 项目交接文档

- 更新日期：2026-08-30
- 工作目录：`/Users/qizs/conclusion/gerclaw/gerclaw-harness`
- 当前分支：`codex/cordis-remediation`
- 当前阶段：五个阶段均已验收，阶段五提交待记录
- 持续进展：[GERCLAW_EXECUTION_PROGRESS.md](GERCLAW_EXECUTION_PROGRESS.md)

## 1. 当前结论

五个阶段均已通过对应真实 GerClaw 网关的 Playwright（浏览器自动化工具）验收。医生、
患者和游客完整路径已闭合，16 个可替换服务已通过真实 Loader（加载器）生命周期矩阵，
应用内浏览器三身份复验、最终构建及 48 项全量工程门禁均已退出成功。

两个固定账号的密码已经用户明确授权，与根目录安全配置同步；医生和患者均已真实登录
成功，密码不再是阻塞项。账号目录、恢复码字段和历史医疗数据均保留。

## 2. 不得回退的产品与架构基线

- 普通用户界面只显示 GerClaw 产品语义，不暴露工作区选择、底座品牌、插件结构、
  原始命令、工具参数、内部地址、密钥、堆栈或开发者术语；保留当前模型选择器。
- 医生和患者功能完全一致，身份只用于租户隔离；游客退出只清理游客专用目录、会话和
  进程，不删除持久账号及历史数据。
- 计划和目标继续使用 DSH（深度求索智能体底座）原生状态与权限规则；目标修改必须
  来自直接用户轮次。
- 语音继续复用 `SpeechProvider`、通义千问提供方、底部麦克风和通用上传入口；不新增
  独立语音页、按钮或框架。
- 五大处方继续在对话中单轮单问、最多五轮，只生成一张包含五章的最终结果卡。
- 共享与私有医学资料继续复用 `dsh-library`、现有文档解析、硅基流动向量嵌入和重排序；
  不建立第二套检索运行时。
- 所有插件启停、更新和卸载必须经 Cordis（插件运行框架）原生 Profile（配置档案）、
  Bundle（组合包）和 Loader；资源由 `ctx.effect()`、`ctx.on()` 或原生可撤销注册拥有。

## 3. 已形成的阶段提交

| 阶段 | 结果 | 提交 |
| --- | --- | --- |
| 一：产品壳层与主对话 | 游客基线 6/6、双账号 2/2、加载器 7/7 通过 | `3ec56fd` |
| 二：语音与五大处方 | 三身份浏览器 3/3、加载器 3/3 通过 | `9b586d4` |
| 三：共享与私有医学资料 | 浏览器 5/5、真实外部检索和加载器 1/1 通过 | `f63ce11` |
| 四：账号隔离与七种产物 | 浏览器 4/4、加载器 5/5 通过 | `e815377` |
| 五：热插拔与最终收口 | 三身份完整路径、加载器 29/29、工程门禁 48/48 通过 | 待记录 |

阶段提交信息依次为：

```text
fix(web): stabilize GerClaw product shell
fix(health): complete voice and prescription flow
fix(rag): complete shared and private retrieval
fix(tenant): close isolation and artifact workflows
chore: complete GerClaw acceptance
```

## 4. 阶段五最终证据

- DSH Market 页面在本阶段开发前重新检索时显示收录 0 个插件；没有新增市场插件。
- 已重新阅读 Cordis 生命周期、组合与热更新文档，并对照 DSH 架构指南核对接入。
- 真实加载器组合共 6 个测试文件、29 项测试通过，覆盖 16 个 GerClaw 可替换服务的
  活动、依赖消失、等待或卸载、级联停止、资源释放、恢复、失败更新回退和无重复注册；
  测试通过真实 `boot` 和加载器树执行，没有直接调用 `apply()`。
- 受阶段五最小修正影响的 5 个测试文件、47 项局部回归通过。
- `pnpm gerclaw:dump-config`、`pnpm typecheck`、`pnpm lint`、`pnpm docs:check`、
  `pnpm verify-md-links`、`pnpm verify-md-wrap` 和 `git diff --check` 已退出成功。
- 完整浏览器文件首次运行 20 项中 17 项通过、2 项失败、1 项按环境跳过；修复长会话
  中问题卡定位根因后，医生、患者和游客完整路径分别成功复跑。首次成功项与三身份
  最终成功复跑共同覆盖全部可运行用例，没有把首次失败计作通过。
- 最终基线浏览器复验为 6 项通过、1 项按环境跳过；应用内浏览器已分别验证游客、医生
  和患者的产品壳层、五项健康入口、模型选择器、语音、上传及主模型真实回复。
- `pnpm build` 退出成功；最终 `pnpm check:all` 为 48 项通过、0 项失败、0 项跳过，
  包含完整测试、构建、266 个源码与 266 个编译后约束伴随项及 0 个重复代码块。
- 旧失败截图、运行日志和缓存已移入
  `/Users/qizs/.Trash/gerclaw-final-cleanup-20260830-0904/`，可恢复；游客目录为空，两个
  固定账号和历史医疗数据未删除。

## 5. 数据与版本控制边界

- 必须保留 `gc_doctor_test_20260823`、`gc_patient_test_20260823` 及其全部历史医疗数据。
- 密码只从 `GERCLAW_TEST_DOCTOR_PASSWORD` 和 `GERCLAW_TEST_PATIENT_PASSWORD` 读取；
  不得输出变量值或把 `.env` 加入 Git（版本控制）。
- `.playwright-cli/`、`dsh/`、`output/`、`.gerclaw/`、账号数据、密钥、日志、缓存和构建
  产物不得提交。
- 阶段五正式源码只在全部验收通过后提交；禁止重置、覆盖历史或建立未验收快照。
- 不配置远程仓库，不推送，不创建合并请求。

## 6. 剩余版本动作

复查差异与敏感文件，只暂存阶段五正式路径，提交
`chore: complete GerClaw acceptance`，然后把提交号回填到本文件和持续进展文档。
